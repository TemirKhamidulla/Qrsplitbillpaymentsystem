
  # QR Split-Bill Payment System

  This is a code bundle for QR Split-Bill Payment System. The original project is available at https://www.figma.com/design/lBqPj14SteWw5CvoAtIUlD/QR-Split-Bill-Payment-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  